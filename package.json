{
  "name": "db-schema-figma-plugin",
  "version": "1.0.0",
  "description": "Figma plugin to visualize database schemas as tables and relationships.",
  "main": "code.ts",
  "scripts": {
    "build": "tsc"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.113.0",
    "typescript": "^5.0.0"
  }
}
